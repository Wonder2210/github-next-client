import * as React from 'react';

import CardHeader from './CardHeader';

export default {
    title:'github-next-client/Molecules/Card header'
};

export const Card_Header = () => {
    return (
        <CardHeader IssuesCount={2345} StarsCount={2345}/>
    )
};
